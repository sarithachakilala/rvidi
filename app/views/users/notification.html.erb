<div class="login_container">
  <div class="span6 margin_bottom_10"> 
    <h1> Notifications</h1>
  </div>
  <div class="row-fluid">
    <% @notifications.each do |each_notification| %>
      <% user= User.find(each_notification.from_id) %>
      <%= user.username%>
    <% end %>
  </div>
</div>